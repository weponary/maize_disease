import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#fffff",
      light: "#fffff",
      dark: "#fffff",
    },
    secondary: {
      main: "#C15319",
      light: "#E37C4D",
      dark: "#8C3510",
    },
  },
});
